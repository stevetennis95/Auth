<h1> Here are the fucking articles!!! </h1>

<p>
<%= link_to "Create new article!", new_article_path %>
<table>
<tr>
	<th>Title</th>
	<th>Post</th>
</tr>

<% @articles.each do |article| %>

<tr>
	<td><%= article.title %></td>
	<td><%= article.body %></td>
	<td><%= link_to "Edit", edit_article_path(article) %></td>
	<td><%= link_to "Show", article_path(article) %></td
</tr>
<% end %>
</table>
